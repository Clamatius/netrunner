[project]
name = "nan"
version = "0.1.0"
description = "Netrunner Algebraic Notation - compact game notation for AI analysis"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
