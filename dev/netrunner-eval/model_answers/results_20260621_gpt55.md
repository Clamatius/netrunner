# Netrunner Reasoning Eval — GPT-5.5 (blind run) + Opus 4.8 head-to-head

**Date:** 2026-06-21
**Model:** GPT-5.5, `model_reasoning_effort=high`, via `codex exec` (default model).
Fresh/blind: isolated dir, read-only sandbox, no answer keys, no repo access — fed only the `build-eval` output inline.
**Set:** all 22 problems, **6-point tutorial set** (identical to the Opus 4.8 run on 2026-06-20, so directly comparable).
**Scorer:** Opus 4.8 (autonomous polish round) against `problems/*-a.md`, with disagreements verified against the q-files + `card_lookup.json`.
**Raw:** `model_answers/gpt_5.5.txt`.

## Headline

| Bucket | Count | Problems |
|--------|-------|----------|
| **Fully correct** | 11 | break-001, mull-001/002/003/004, partial-001, remote-002, turn1-001, turn1-003, window-001, yomi-001 |
| **Partial** | 7 | breach-001, multirun-001, score-001, scoring-002, servers-001, turn1-002, turn1-004 |
| **Wrong** | 3 | lethal-001, midgame-001, remote-001 |
| *(excluded)* | 1 | trace-001 — out-of-scope recall problem ([082]); GPT got Q1/Q2, missed the Archived-Memories recursion on Q3 |

**In-scope = 21 (trace-001 excluded for both models).**
Strict (fully-correct only): **11/21 ≈ 52%.** Lenient (partial = ½): **14.5/21 ≈ 69%.**

## Head-to-head (same 21-problem 6-point set, same scorer)

| Model | Fully | Partial | Wrong | Strict | Lenient |
|-------|-------|---------|-------|--------|---------|
| **Claude Opus 4.8** | 13 | 5 | 3 | **62%** (13/21) | **74%** |
| **GPT-5.5 (high)** | 11 | 7 | 3 | **52%** (11/21) | **69%** |

Opus 4.8 edges GPT-5.5 on this set. The gap is two problems where Opus was clean and GPT was partial: **servers-001** (Opus found the full 6-access Jailbreak chain; GPT stopped at 4 and wasted a click) and **score-001** (Opus stated the clean install-now/Seamless-next-turn win; GPT installed the Hub but buried it under an unnecessary Government Subsidy line and wasted a click advancing). Same 3 problems wrong for both — but for *different reasons* (below). Caveat: one scorer (Opus 4.8); some partial/correct boundaries are judgment calls, flagged inline.

> Context: the 2026-01-06 run on the older **11-problem** subset had GPT-5.1 High at 91% and Opus 4.5 at 73%. **Not comparable** — this 22-set is larger and Hard-weighted, and the prior subset omitted most of the trap/tempo problems that separate the models here.

## The interesting part — same wrong answers, different failure modes

### midgame-001 — GPT did NOT have the tempo-blindness; Opus did
This is the problem the project cares about most (the [070]/[069] threat-clock axis).

- **Opus 4.8** wrote *"Send a Message needs 5 advancements, has 2, **not scorable soon**"* and built economy. Classic threat-clock blindness — it never registered that Corp scores next turn.
- **GPT-5.5** explicitly wrote *"Because tutorial is to 6 and Corp at 3, **Send a Message would win**."* **It counted the threat clock correctly.** Its error was downstream: a **click-sequencing miscalculation**. It planned economy on clicks 1–2 (Telework, Pennyshaver), which left only clicks 3 (run) + 4 (one bioroid break) — not enough to click through all 3 of Brân's subroutines — so it concluded *"the exact run line appears impossible"* and defaulted to building. It set confidence **LOW** and flagged the tension itself.

The fix the model missed: **run on click 1**, then clicks 2/3/4 break all three Brân subs via the Bioroid "lose [click]: break 1 sub" ability, then break Palisade with Cleaver from the $3 pool ($2 boost + $1 = $0). The key's whole point — *"getting in broke is better than getting in rich if it keeps the remote small"* — is a sequencing insight, and GPT inverted the sequence (econ-first).

**This is a sharper, more useful datapoint than Opus's miss.** GPT's blind spot here is not "is the opponent threatening?" (it nailed that) but "what's the cheapest click-ordering to contest, and can I pay ICE with clicks instead of credits?" That's a different doc target: the bioroid click-break + run-first-then-bank ordering, not the threat-clock check.

### remote-001 — GPT shared Opus's tax-vs-wall blind spot
- The trap: a 4-ICE remote vs a Runner with no Killer/Decoder and $2 *looks* unbreachable. But Whitespace only ETRs at ≤$6 and Tithe is 1 net damage — neither is a hard wall — and Red Team + Pennyshaver bank to ~$11 in one turn to tank through and break the two Palisades with Cleaver.
- **Both models said "the remote is safe, install Offworld."** GPT additionally mis-ruled that 1 net damage to a 1-card grip is a *flatline* (it isn't — you discard to 0 cards and survive). Same core error as Opus: treated breaker gaps as walls and never checked whether the subroutines actually end the run. This is the "subroutines are a tax, not a wall" lesson with the text in front of the model — a **shared model blind spot**, high signal.

### lethal-001 (hard) — both wrong, GPT got one sub-insight
Guaranteed line: Archives (Carmen discount) → install Carmen → run HQ; **on access, trash Manegarm Skunkworks** to remove it from HQ so the second access is a *guaranteed* agenda. Without the trash you get 75%, not certainty (HQ access is random *with replacement*).
- **GPT-5.5** found the Archives discount (Opus missed it) but then ran HQ twice **without** trashing Manegarm and wrongly claimed two accesses guarantee both cards. It missed the trash-to-guarantee — same end-state miss as Opus, slightly different path.

## GPT-5.5 behavioral signature (vs Opus)

- **"Declares impossible/inconsistent" tic.** GPT flagged breach-001 Q3, midgame-001, score-001, and trace-001 Q3 as impossible/internally-inconsistent. Sometimes right (trace-001 Q3 genuinely needs out-of-scope card recall) but twice it was a cover for a sequencing miss it couldn't find (midgame-001, breach-001 Q3 — both *do* have legal lines). Opus more often committed to a concrete (sometimes wrong) line. Net: GPT under-commits when the line is tight; Opus over-commits.
- **Bioroid click-break under-used.** The midgame-001 and breach-001 misses both stem from not leaning on "pay the bioroid with clicks, bank credits for the credit-only ICE." GPT reached for Cleaver/credits on Brân first.
- **No new doc bugs.** Opus's run surfaced 2 genuine answer-key errors; GPT's run surfaced none. The 3 it got wrong are model errors, not key errors (all three keys verified correct this pass).
- **Strengths shared with Opus:** all 4 mulligans correct; Seamless "did not install this turn" respected every time (no same-turn fast-advance illusions); window-001 and yomi-001 (the "threat of an agenda is a resource" leveling game) both clean. partial-001 partial-break lesson clean.

## Takeaways for the project

1. **The tempo finding splits by model.** Opus's gap is the *threat clock itself* ("is the opponent about to win?"). GPT-5.5's gap on the same problem is *one layer down*: given the threat is seen, find the cheapest click-ordering to contest (run-first, pay bioroids with clicks). A single playbook addition could help both, but they need different emphases — Opus needs "ask the threat-clock question first," GPT needs "bioroid = click tax; sequence the run before banking."
2. **remote-001's tax-vs-wall trap is the one shared blind spot.** Both top models fail it with full text in front of them. Strongest candidate for a first-class "does this subroutine actually end the run?" triage step in the playbooks.
3. **Head-to-head verdict (this set):** Opus 4.8 > GPT-5.5, 62% vs 52% strict. Narrow, single-scorer, tutorial-only — directional, not definitive. A live Corp-vs-Runner marquee game between the two remains the real test; the eval isolates *reasoning-under-provided-context*, not in-game execution.
